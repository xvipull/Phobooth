{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run cat rainbow generator",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/app.py",
      "console": "integratedTerminal",
      "env": {
        "HF_TOKEN": "${env:HF_TOKEN}"
      }
    }
  ]
}
